PORT=3001
NODE_ENV=development

# Tesseract worker pool size
WORKER_POOL_SIZE=2

# Upload limits
MAX_FILE_SIZE_MB=10

# Confidence threshold (0-100) — reject reads below this
MIN_CONFIDENCE=45

# Cleanup processed images after N ms (0 = keep forever)
CLEANUP_DELAY_MS=30000
